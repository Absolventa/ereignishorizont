.container
  - flash.each do |name, msg|
    %div{:class => "alert alert-#{name == :notice ? "success" : "error"}"}
      %a.close{"data-dismiss" => "alert"} x
      = msg

%h1
  Remote Side

%table.table.table-striped{"data-provides" => "rowlink"}
  %thead
    %tr
      %th= sortable "name"
      %th API Token
      %th
      %th

  %tbody
    - @remote_sides.each do |remote_side|
      %tr
        %td= link_to remote_side.name, remote_side
        %td= remote_side.api_token
        %td.nolink= link_to icon_pencil, edit_remote_side_path(remote_side)
        %td.nolink= link_to icon_trash, remote_side, method: :delete, data: { confirm: 'Are you sure?' }

= link_to 'New Remote Side', new_remote_side_path(@remote_side) , class: 'btn btn-custom-success'
= link_to 'Back', remote_sides_path , class: 'btn btn-custom-back'