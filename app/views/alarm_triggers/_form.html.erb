<%= form_for(@alarm_trigger) do |f| %>
  <% if @alarm_trigger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alarm_trigger.errors.count, "error") %> prohibited this alarm_trigger from being saved:</h2>

      <ul>
      <% @alarm_trigger.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </p>

  <p>
    <%= f.label :action %><br>
    <%= f.select :action, [['SMS', 'sms'], ['Email', 'email'], ['Carrier Pidgeon', 'carrierpidgeon'], ['Message in a bottle', 'messageinabottle']] %>  </p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>